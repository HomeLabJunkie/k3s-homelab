# Repository-supported automation toolchain.
# ANSIBLE_CORE_* must stay aligned with the ansible-core constraint in
# requirements.in; scripts/check-requirements-toolchain-sync.sh enforces this.
ANSIBLE_CORE_MIN=2.21.3
ANSIBLE_CORE_MAX_EXCLUSIVE=2.22.0
PYTHON_MIN=3.11.0
PYTHON_MAX_EXCLUSIVE=3.15.0
