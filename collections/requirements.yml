---
# Known-good collection set validated with ansible-core 2.20.1 / Ansible 13.1.0.
# Pin exact versions so ~/.ansible/collections does not drift between hosts/runs.
collections:
  - name: ansible.utils
    version: "6.0.0"
  - name: community.general
    version: "10.5.0"
  - name: ansible.posix
    version: "2.0.0"
  - name: kubernetes.core
    version: "5.2.0"
