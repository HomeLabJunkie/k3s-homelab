---
- name: Update Debian-family hosts
  hosts: "*"
  become: true
  tasks:
    - name: Update apt cache and upgrade installed packages
      ansible.builtin.apt:
        update_cache: true
        upgrade: "yes"
